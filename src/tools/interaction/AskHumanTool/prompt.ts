export const DESCRIPTION = `Ask the human operator a yes/no question and get their response. Returns 'approved' or 'denied'. When requesting permission for a local write, execution, or durable run, set approval_scope and approval_target with the concrete tool_name and exact arguments for the intended request. Approval unlocks only that matching request; changed arguments require a new approval.`;
