export const DESCRIPTION =
  "Recall goal-independent agent memory. Freeform recall uses semantic mode by default; exact key lookup and lexical substring lookup must be requested explicitly with mode='exact' or mode='lexical'. Returns matching memory entries using tiered retrieval: compiled entries (with summaries) are returned first, followed by raw entries, both sorted by most recently updated. Archived entries are excluded by default (use include_archived=true to include them). Use this to recall previously saved facts, procedures, preferences, or observations without treating substring matches as the primary freeform memory path.";
